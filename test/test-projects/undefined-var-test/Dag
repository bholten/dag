;; Test that undefined namespace variables cause a hard failure

project undef_var_test {
    let BUILD_DIR build

    task bad_var {
        description "Uses a misspelled variable"
        run {
            echo ${undef_var_test::BUILD_DIER}
        }
    }
}
